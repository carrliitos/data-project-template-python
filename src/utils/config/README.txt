The following configuration files are needed:
    1. tableau_project_ids.json which holds the Project IDs for all Tableau folders
    2. config.json which holds database configurations
    3. emailconfig.ini which hols email credentials
